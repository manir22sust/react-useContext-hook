import { Auth } from "./components/Auth";

const App = () => {
  //using the state to dynamicallly pass the values to the context

  return (
    <>
      <Auth />
    </>
  );
};
export default App;
